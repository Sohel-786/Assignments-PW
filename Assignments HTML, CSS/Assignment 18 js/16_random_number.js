(() => {
    let random_num = Math.ceil(Math.random() * 100)
    // while(random_num <= 100){
    //     console.log(random_num);
    //     random_num = Math.ceil(Math.random() * 100) // This is just for confirmation
    // }
    console.log(random_num);
})();