let length = 10;
let width = 20;

let perimeter = 2 * (length + width);
console.log(`The perimeter of the rectangle with length: ${length} and width: ${width} is ${perimeter}`);

// Output : The perimeter of the rectangle with length: 10 and width: 20 is 60